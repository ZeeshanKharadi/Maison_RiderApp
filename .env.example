# Copy to `.env` in the project root (same folder as package.json).
# Never commit `.env` — it is gitignored.

# Google Maps SDK key (Maps SDK for Android + iOS if using Google provider)
GOOGLE_MAPS_API_KEY=
